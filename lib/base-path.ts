/**
 * The sub-path GitHub Pages serves this site under. It must match the
 * repository name's casing exactly (https://ofirer92.github.io/Tomerkk/),
 * because Pages treats paths as case-sensitive.
 *
 * Imported by next.config.ts (for routing and assets) and by lib/contact.ts
 * (for image URLs), so there is only one place to change it.
 */
export const BASE_PATH = "/Tomerkk";
export const SITE_URL = `https://ofirer92.github.io${BASE_PATH}`;
