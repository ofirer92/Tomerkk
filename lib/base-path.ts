/**
 * The sub-path GitHub Pages serves this site under. Empty when the site is
 * served from a custom domain's root (kabriz.com) rather than from
 * https://ofirer92.github.io/Tomerkk/.
 *
 * Imported by next.config.ts (for routing and assets) and by lib/contact.ts
 * (for image URLs), so there is only one place to change it.
 */
export const BASE_PATH = "";
export const SITE_URL = "https://kabriz.com";
